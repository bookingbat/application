You have used <?= $this->escape($this->classes_used) ?> classes this month. Guest users must pay for clases. Here is the payment form

<?php
if ($this->form->isErrors()) {
    foreach ($this->form->getErrorMessages() as $errormessage) {
        echo '<span class="error">' . $this->escape($errormessage) . '</span>';
    }
}
echo $this->form;
?>